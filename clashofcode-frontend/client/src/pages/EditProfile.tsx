/**
 * Style system: ClashOfCode Tournament Console — profile editing keeps personal controls
 * grounded in the same dossier panels, red match-line accents, and concise mono metadata.
 */
import { Avatar, MatchLine, Pill, RankBadge, StatBlock } from "@/components/ArenaPrimitives";
import { player } from "@/data/mockData";
import { ArrowLeft, Check, Eye, MapPin, Save, Shield, UserRound } from "lucide-react";
import { FormEvent, useState } from "react";
import { Link } from "wouter";

export default function EditProfile() {
  const [saved, setSaved] = useState(false);
  const [form, setForm] = useState({ name: player.name, handle: player.handle, location: "Mumbai, IN", bio: "Algorithmic thinker. Quiet until the timer starts.", visibility: "public" });
  const update = (key: keyof typeof form, value: string) => { setSaved(false); setForm((current) => ({ ...current, [key]: value })); };
  const submit = (event: FormEvent) => { event.preventDefault(); setSaved(true); };

  return <div className="page-wrap enter-up"><section className="border border-white/10 bg-[#18191f] p-5 sm:p-7"><MatchLine label="Player control / edit dossier" /><div className="mt-5 flex flex-wrap items-center justify-between gap-3"><Link href="/profile" className="inline-flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.1em] text-[#a7aab4] transition-colors hover:text-white"><ArrowLeft className="h-3.5 w-3.5" />Back to profile</Link><Pill tone="red">Identity settings</Pill></div><div className="mt-7 flex flex-col gap-5 sm:flex-row sm:items-center"><Avatar initials={player.initials} size="lg" /><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h1 className="font-display text-3xl font-bold tracking-[-.065em]">Edit your arena identity</h1><RankBadge rank={player.rank} /></div><p className="mt-2 max-w-xl text-sm leading-6 text-[#aeb1ba]">Tune the player details your opponents see before the countdown starts.</p></div></div><div className="mt-7 grid grid-cols-2 gap-x-6 gap-y-6 border-t border-white/[.08] pt-5 sm:grid-cols-4"><StatBlock label="Rating" value={player.rating} detail="current signal" accent /><StatBlock label="Level" value={player.level} detail="4,020 XP" /><StatBlock label="Battles" value={player.battles} detail="season total" /><StatBlock label="Win rate" value={`${player.winRate}%`} detail="42 victories" /></div></section><div className="mt-5 grid gap-5 xl:grid-cols-[minmax(0,1.35fr)_minmax(320px,.65fr)]"><form onSubmit={submit} className="panel p-5 sm:p-7"><div className="flex items-end justify-between gap-4"><div><p className="section-kicker">Public player card</p><h2 className="mt-1 font-display text-2xl font-bold tracking-[-.055em]">Profile information</h2></div>{saved && <span className="inline-flex items-center gap-1.5 font-mono text-[10px] uppercase tracking-[.08em] text-[#cde7a5]"><Check className="h-3.5 w-3.5" />Saved locally</span>}</div><div className="mt-7 grid gap-5 sm:grid-cols-2"><label className="text-xs font-semibold text-[#d1d3d8]">Display name<input value={form.name} onChange={(event) => update("name", event.target.value)} className="mt-2 w-full border border-white/[.12] bg-[#101116] px-3.5 py-3 text-sm text-[#f3f0ea] outline-none transition-colors focus:border-[#f04432]" /></label><label className="text-xs font-semibold text-[#d1d3d8]">Player handle<input value={form.handle} onChange={(event) => update("handle", event.target.value)} className="mt-2 w-full border border-white/[.12] bg-[#101116] px-3.5 py-3 text-sm text-[#f3f0ea] outline-none transition-colors focus:border-[#f04432]" /></label><label className="text-xs font-semibold text-[#d1d3d8] sm:col-span-2">Location<input value={form.location} onChange={(event) => update("location", event.target.value)} className="mt-2 w-full border border-white/[.12] bg-[#101116] px-3.5 py-3 text-sm text-[#f3f0ea] outline-none transition-colors focus:border-[#f04432]" /></label><label className="text-xs font-semibold text-[#d1d3d8] sm:col-span-2">Bio<textarea value={form.bio} onChange={(event) => update("bio", event.target.value)} rows={4} className="mt-2 w-full resize-y border border-white/[.12] bg-[#101116] px-3.5 py-3 text-sm leading-6 text-[#f3f0ea] outline-none transition-colors focus:border-[#f04432]" /></label></div><div className="mt-7 flex justify-end border-t border-white/[.08] pt-5"><button type="submit" className="primary-button"><Save className="h-4 w-4" />Save profile</button></div></form><aside className="space-y-5"><section className="panel p-5"><div className="flex items-center gap-2"><Eye className="h-4 w-4 text-[#b5df73]" /><p className="section-kicker">Profile visibility</p></div><h2 className="mt-2 font-display text-xl font-bold tracking-[-.05em]">Choose your signal.</h2><div className="mt-5 space-y-2">{["public", "friends", "private"].map((visibility) => <button type="button" onClick={() => update("visibility", visibility)} key={visibility} className={`w-full border p-3 text-left transition-colors ${form.visibility === visibility ? "border-[#f04432]/70 bg-[#f04432]/[.08]" : "border-white/[.09] bg-white/[.025] hover:border-white/20"}`}><span className="flex items-center justify-between"><span className="text-xs font-semibold capitalize text-[#e7e7e9]">{visibility === "friends" ? "Friends only" : visibility}</span>{form.visibility === visibility && <span className="h-2 w-2 rotate-45 bg-[#f04432]" />}</span><span className="mt-1 block text-[11px] leading-4 text-[#8e919a]">{visibility === "public" ? "Visible on leaderboards and player cards." : visibility === "friends" ? "Visible to connected rivals and teammates." : "Shown only to you in the arena."}</span></button>)}</div></section><section className="panel p-5"><div className="flex items-center gap-2"><Shield className="h-4 w-4 text-[#e1a759]" /><p className="section-kicker">Account signal</p></div><div className="mt-5 space-y-3 text-xs text-[#a7aab3]"><p className="flex items-center gap-2"><UserRound className="h-3.5 w-3.5 text-[#858893]" />Email changes require account verification.</p><p className="flex items-center gap-2"><MapPin className="h-3.5 w-3.5 text-[#858893]" />Location appears only on your public dossier.</p></div></section></aside></div></div>;
}
